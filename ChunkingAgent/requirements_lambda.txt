# AWS Lambda Requirements for IntelliAudit Chunking Agent
# Optimized for Lambda deployment with size and performance considerations

# Core AWS SDKs (boto3 is included in Lambda runtime, but we specify for local testing)
boto3>=1.34.0
botocore>=1.34.0

# Data processing libraries
pandas>=2.0.0
numpy>=1.24.0

# Document processing
python-docx>=0.8.11
PyPDF2>=3.0.0
openpyxl>=3.1.0

# Text processing and ML (optimized versions)
nltk>=3.8.1
scikit-learn>=1.3.0
chardet>=5.0.0

# Note: Removed heavy dependencies for Lambda optimization:
# - sentence-transformers (large model files)
# - langgraph, langchain (not needed for core chunking functionality)
# - These can be added back if needed via Lambda Layers

# Lambda-specific considerations:
# 1. Total deployment package size limit: 250MB (uncompressed)
# 2. /tmp directory limit: 10GB (for NLTK data)
# 3. Memory limit: 128MB - 10GB
# 4. Timeout limit: 15 minutes
